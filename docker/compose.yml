name: heladix

services:

  postgres:
    image: postgres:18
    container_name: heladix-postgres

    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

    ports:
      - "${POSTGRES_PORT}:5432"

    volumes:
      - heladix-postgres-data:/var/lib/postgresql

volumes:
  heladix-postgres-data: